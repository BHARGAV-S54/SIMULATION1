{
  "name": "drone-sim-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node node_modules/vite/bin/vite.js build",
    "preview": "vite preview",
    "postinstall": "node -e \"if (process.platform !== 'win32') require('child_process').execSync('chmod -R 755 node_modules/.bin', {stdio: 'ignore'})\""
  },
  "dependencies": {
    "three": "^0.168.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "vite": "^8.0.3"
  }
}
